import 'package:flutter/material.dart';

class CaloriasAltas extends StatelessWidget {
  const CaloriasAltas({super.key});

  @override
  Widget build(BuildContext context) {
    return const AlertDialog(
      title: Text('Alto en Calorias'),
      content: Text(''),
    );
  }
}

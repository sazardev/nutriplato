import 'package:flutter/material.dart';

class ProteinasAltas extends StatelessWidget {
  const ProteinasAltas({super.key});

  @override
  Widget build(BuildContext context) {
    return const AlertDialog(
      title: Text('Alto en Proteinas'),
      content: Text(''),
    );
  }
}
